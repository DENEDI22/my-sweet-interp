var int i = 0;
var list[int] arr = []; 
loop {
    match i < 10 {
        true {
            arr.push(i);
            print(arr[i]);
            i = i + 1;
        }
        _ {
            break
        }
    }
}
